const { Client } = require('pg'); async function test(host) { const client = new Client({ connectionString: 'postgresql://postgres:postgres@' + host + ':5432/chioi?schema=public', connectionTimeoutMillis: 3000 }); try { await client.connect(); console.log('Connected to', host); await client.end(); } catch(e) { console.error('Failed', host, e.message); } } async function main() { await test('localhost'); await test('127.0.0.1'); await test('[::1]'); } main();
